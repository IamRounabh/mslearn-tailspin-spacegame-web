trigger:
- main
pool: 
    name: Default
variables:
    buildConfiguration: 'Release'
steps:
- script: dotnet build --configuration (buildConfiguration)
  display: 'dotnet build --configuration (buildConfiguration)'